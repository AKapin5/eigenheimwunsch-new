rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
  
    match /Clients/{document=**} {
      allow read, write;
    }
    
    match /Clients_only_contact/{document=**} {
      allow read, write;
    }
    
  }
}